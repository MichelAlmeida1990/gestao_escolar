# alunos/urls.py
from django.urls import path
from . import views

app_name = 'alunos'

urlpatterns = [
    # Por enquanto, vamos deixar vazio
    # Adicionaremos as URLs à medida que criarmos as views
]
